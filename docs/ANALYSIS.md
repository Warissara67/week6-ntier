# 📊 ANALYSIS – N-Tier Architecture (Week 6)

ผู้จัดทำโดย
67543210073-2 (นางสาว วริศรา สรรพกรพิเศษ)

เอกสารฉบับนี้จัดทำขึ้นเพื่อวิเคราะห์และสรุปผลการทดลองการพัฒนาและทดสอบระบบตามแนวคิด 
**N-Tier Architecture** โดยอ้างอิงจากการปฏิบัติจริงใน Lab Week 6 รายวิชา 
**ENGSE207** สามารถนำไปใช้งานเป็นไฟล์ ANALYSIS.md ได้ทันที

---

## 1. ทฤษฎีที่เกี่ยวข้อง (Related Theories)

### 1.1 N-Tier Architecture

N-Tier Architecture คือรูปแบบสถาปัตยกรรมซอฟต์แวร์ที่แบ่งระบบออกเป็นหลายชั้น (Tier) โดยแต่ละชั้นมีหน้าที่รับผิดชอบแตกต่างกันอย่างชัดเจน เพื่อเพิ่มความยืดหยุ่น ความสามารถในการดูแลรักษา และความปลอดภัยของระบบ

โดยทั่วไป N-Tier Architecture จะประกอบด้วย:

* **Presentation Tier** : ส่วนติดต่อผู้ใช้ (เช่น Web Browser, Frontend)
* **Application / Business Logic Tier** : ประมวลผลกฎทางธุรกิจและควบคุมการทำงานของระบบ
* **Data Tier** : จัดการข้อมูลและฐานข้อมูล (Database)

การแยกชั้นลักษณะนี้ช่วยให้สามารถแก้ไขหรือพัฒนาแต่ละส่วนได้โดยไม่กระทบส่วนอื่นของระบบ

---

### 1.2 Web Server และ Reverse Proxy

Web Server เช่น **Nginx** ทำหน้าที่รับ HTTP Request จากผู้ใช้ และสามารถทำงานเป็น Reverse Proxy เพื่อส่งต่อคำขอไปยัง Application Server ที่อยู่เบื้องหลังได้ ช่วยเพิ่มความปลอดภัยและลดภาระของ Backend

---

### 1.3 Version Control System (Git)

Git เป็นระบบควบคุมเวอร์ชันที่ช่วยติดตามการเปลี่ยนแปลงของซอร์สโค้ด รองรับการทำงานร่วมกันเป็นทีม และสามารถเชื่อมต่อกับ Remote Repository เช่น GitHub เพื่อจัดเก็บและแชร์โค้ด

---

## 2. อธิบายการทำงานของระบบ (Operation Workflow)

ลำดับการทำงานของระบบ N-Tier ที่พัฒนาใน Lab นี้สามารถอธิบายได้ดังนี้:

1. ผู้ใช้ส่งคำขอ (HTTP Request) ผ่าน Web Browser หรือคำสั่ง `curl`
2. Request ถูกส่งมายัง **Nginx (Web Server / Presentation Tier)**
3. Nginx ทำหน้าที่เป็น Reverse Proxy ส่ง Request ต่อไปยัง **Application Tier**
4. Application Tier ประมวลผล Logic และติดต่อกับ **Database (Data Tier)** หากจำเป็น
5. Database ส่งข้อมูลกลับไปยัง Application Tier
6. Application Tier ส่ง Response กลับไปยัง Nginx
7. Nginx ส่งผลลัพธ์สุดท้ายกลับไปยังผู้ใช้

โครงสร้างนี้ช่วยให้ระบบมีความชัดเจน แยกหน้าที่ และรองรับการขยายระบบในอนาคตได้ง่าย

---

## 3. การวิเคราะห์ปัญหาและแนวทางแก้ไข (Troubleshooting Analysis)

จากการทดลอง พบปัญหาที่เกิดขึ้นจริงและแนวทางแก้ไขดังนี้:

### 3.1 ปัญหา: `git commit` ไม่ได้ (Author identity unknown)

**สาเหตุ:** ยังไม่ได้ตั้งค่า user.name และ user.email ของ Git

**แนวทางแก้ไข:**

```bash
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

---

### 3.2 ปัญหา: เพิ่ม remote ไม่ได้ (`remote origin already exists`)

**สาเหตุ:** มีการตั้งค่า remote origin ไว้แล้ว

**แนวทางแก้ไข:**

```bash
git remote -v
git remote remove origin
git remote add origin <repository_url>
```

---

### 3.3 ปัญหา: curl แล้วได้ `301 Moved Permanently`

**สาเหตุ:** Nginx redirect จาก HTTP ไป HTTPS

**แนวทางแก้ไข:**

```bash
curl -k https://localhost
```

หรือกำหนด URL ให้ตรงกับ protocol ที่ตั้งค่าไว้

---

## 4. สรุปผลการวิเคราะห์ (Conclusion)

จากการทดลองพัฒนาและทดสอบระบบตามแนวคิด **N-Tier Architecture** พบว่าโครงสร้างแบบแยกชั้นช่วยให้ระบบมีความเป็นระเบียบ เข้าใจง่าย และสามารถแก้ไขปัญหาเฉพาะจุดได้สะดวก การใช้ Nginx ร่วมกับ Application Server และ Database ช่วยเพิ่มประสิทธิภาพและความปลอดภัยของระบบ

นอกจากนี้ การใช้ Git และ GitHub ช่วยให้การจัดการซอร์สโค้ดเป็นระบบ ลดความผิดพลาด และรองรับการทำงานร่วมกันได้ดี ซึ่งสอดคล้องกับหลักการพัฒนาซอฟต์แวร์เชิงวิศวกรรม

โดยรวม การทดลองครั้งนี้ช่วยให้เข้าใจการทำงานของระบบแบบ N-Tier ทั้งในเชิงทฤษฎีและการปฏิบัติจริงอย่างชัดเจน และสามารถนำความรู้ไปประยุกต์ใช้กับระบบขนาดใหญ่ในอนาคตได้

---
## 5. Screenshot
<img width="1119" height="634" alt="Screenshot 2569-01-19 at 14 06 31" src="https://github.com/user-attachments/assets/967d7b16-afe7-4d83-9516-e0ffbf6502e4" />
<img width="1128" height="771" alt="Screenshot 2569-01-19 at 14 06 54" src="https://github.com/user-attachments/assets/ecb6db7a-eaa5-4a05-a4e4-3e7b1edd9688" />


📌 *เอกสารนี้จัดทำเพื่อใช้ประกอบการเรียนการสอนและการส่งงาน Lab รายวิชา ENGSE207*
